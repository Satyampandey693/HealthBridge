# Backend origin. Leave empty in development (Vite proxy handles /api).
# In production set to your deployed backend, e.g. https://api.healthbridge.app
VITE_API_URL=

# Socket.io server origin (websockets bypass the Vite proxy).
VITE_SOCKET_URL=http://localhost:5001
